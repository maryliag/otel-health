[project]
name = "otel-health"
version = "0.1.0"
description = "OpenTelemetry community health metrics — triagers, approvers, and maintainers across all repos"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv]
dev-dependencies = []
